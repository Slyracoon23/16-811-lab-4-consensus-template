[project]
name = "lab-consensus"
version = "0.1.0"
description = "Consensus over a robot network, at the rate its graph allows"
requires-python = ">=3.10"
dependencies = ["numpy>=1.26"]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
